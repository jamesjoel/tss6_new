import { ProDiscountPipe } from './pro-discount.pipe';

describe('ProDiscountPipe', () => {
  it('create an instance', () => {
    const pipe = new ProDiscountPipe();
    expect(pipe).toBeTruthy();
  });
});
